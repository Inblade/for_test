# for_test
only for test

#  Технічний звіт по стану сервера 

##  Поточна ситуація на сервері

• **Серйозне перевантаження CPU**: Load Average — **37.66** при 24 потоках CPU. Це **>150% від допустимого рівня**.  
• **Процесор повністю завантажений**:
  - Idle — **0.0%**
  - User — **61.1%**
  - System — **38.1%**
  - Це **CPU-bound** стан — процесор забитий, система нестабільна.

---

##  Що відбувається 

### • Антивірус ClamAV (`clamd`)
- **USER**: `root`  
- **PID**: `3560224`  
- **%CPU**: **105.2%**  
- ➤ Займає понад одне ядро без throttle. Працює безконтрольно, створює додаткове навантаження.

---

### • MariaDB (`mysqld`)
- **USER**: `mysql`  
- **PID**: `3184454`  
- **%CPU**: **90.8%**  
- ➤ Інтенсивні SQL-запити. Можливо, відсутнє кешування, використовуються неіндексовані поля, важкі JOIN-и.

---

### • Фонові сканери `csxwatch`

| PID       | USER | %CPU | Команда               |
|-----------|------|------|------------------------|
| 3290373   | root | 77.8% | `csxwatch - scanning` |
| 3290374   | root | 77.4% | `csxwatch - scanning` |
| 3290376   | root | 72.2% | `csxwatch - scanning` |
| 3290375   | root | 70.9% | `csxwatch - scanning` |

➤ Сумарне навантаження: **~298% CPU** — майже три ядра завантажені сканерами одночасно.

---

### • PHP-процеси (LiteSpeed `lsphp`) — активні WordPress-сайти

---

### • Системний процес `kswapd0`
- **USER**: `root`  
- **PID**: `356`  
- **%CPU**: **96.7%**  
- ➤ Активний свопінг, ймовірно через нестачу оперативної пам’яті.

---

##  Акаунти, що створюють навантаження

➤ Всі акаунти працюють із WordPress. Часто викликаються `index.php`, `wp-cron.php`, `admin-ajax.php`, що типово для бот-атак або неефективної оптимізації.

---

##  Проблема

Навантаження формується з кількох джерел одночасно:
- **ClamAV (`clamd`)** — 105.2% CPU
- **MariaDB (`mysqld`)** — 90.8% CPU
- **`csxwatch`** — ~298% CPU (4 процеси)
- **`lsphp`** — десятки WordPress-процесів 
- **`kswapd0`** — 96.7% CPU (через свопінг)

---

##  Найвужче місце

- **CPU**: 100% завантаження
- **Load Average**: 37+ при 24 потоках
- **Idle**: 0.0%

➤ Система не має резерву ресурсів, існує загроза повного зависання.

---

##  Рішення

- Зупинити **clamd** і **csxwatch**
- Обмежити ресурси для **PHP-процесів**
- Оптимізувати **SQL-запити** та конфігурацію MariaDB
- Увімкнути **OPcache** / **LSCache**
- Встановити **CloudLinux** для ізоляції акаунтів, якщо ще немає??
- Захистити **WordPress-сайти** (wp-cron, xmlrpc.php, ModSecurity)
- Перенести фонове сканування на **нічні години** коли найменше користувачів??
